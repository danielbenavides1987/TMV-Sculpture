{
  "buildCommand": "cd client && npm install && npm run build && cp -r dist ../dist",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
