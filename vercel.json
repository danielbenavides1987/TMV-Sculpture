{
  "buildCommand": "cd client && npm install && npm run build && cp -r dist ../dist_vercel",
  "outputDirectory": "dist_vercel",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
